@model IEnumerable<DancingGoat.Models.SocialLinks.SocialLinkViewModel>

@foreach (var link in Model)
{
    <a class="followus-link" href="@link.Url" target="_blank">
        <img src="@link.IconPath" alt="@link.Title" title="@link.Title" />
    </a>
}

